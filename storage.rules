rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow anyone to view images
      allow read: if true;
      // Only allow uploads and deletions if the user is authenticated
      allow write: if request.auth != null;
    }
  }
}
