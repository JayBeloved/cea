rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read data (so the public site can fetch it)
    match /{document=**} {
      allow read: if true;
      // Only allow writes (create, update, delete) if the user is authenticated
      allow write: if request.auth != null;
    }
  }
}
